# .agentignore — Exclude from AI assistant context
# Goal: keep context focused on source code and project docs

# === Lock files (large, not useful for AI) ===
uv.lock
yarn.lock

# === Binary / generated files ===
docs/design/*.png
*.ico
*.woff
*.woff2

# === Python artifacts ===
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# === Node.js artifacts ===
node_modules/
frontend/dist/

# === Environment & secrets ===
.env
.env.*
!**/.env.example

# === Django generated ===
*.log
db.sqlite3
staticfiles/
media/

# === Testing / coverage ===
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/
coverage.xml

# === IDE / editor ===
.vscode/
.idea/
*.swp
*.swo
*~

# === OS ===
.DS_Store
._*
Thumbs.db

# === Git metadata ===
.git/
.gitignore
.gitattributes
.gitmessage

# === Keep these (do NOT ignore) ===
# migrations/     — schema history, important for understanding models
# .env.example    — documents required env vars (negated above with !)
# Makefile        — documents available commands
# package.json    — documents scripts and dependencies
